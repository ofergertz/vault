# Memory — 2026-05-04

## Docker Desktop 4.7.1 בעיה
- עופר עדכן ל-Docker Desktop 4.7.1 וכל הפודים ב-K8s עלו עם `ImagePullBackOff`
- הבעיה: K8s בתוך DD לא יכול להגיע ל-`127.0.0.1:5000` (local registry) — הוא צריך `host.docker.internal:5000`
- הפתרון: הוסף `"insecure-registries": ["host.docker.internal:5000"]` ל-Docker Engine JSON ב-Settings
- הפרויקט: `C:\EmeraldEx\selling-service`, namespace `nr1-selling`
- ה-registry רץ על פורט 5000 ו-5001

## efsta / return-receipt.pug
- עופר עובד על קבלות החזרה (Pokwitowanie zwrotu) עבור חברת efsta
- הקובץ הסופי נשמר ב: `/data/.openclaw/workspace/return-receipt.pug`
- המבנה הסופי צמוד ל-invoice.pug המקורי של efsta
- Grid: `47ch 1ch 8ch` (3 עמודות) — זהה ל-invoice
- כותרת: `.Title` עם `display:block; text-align:center; font-size:180%; font-weight:bold`
- Item header: שורה אחת `.Lin.Disp` עם `.Dsc` שמכיל את כל הטקסט השמאלי
- `taxPercentByGroup` משתמש ב-`parseFloat` כדי למנוע `8.000000%`
- ESR fields ב-TBR request: `D, D0, TL, TT, TN, T, Opr, OprN, OprTIN, NFS, RFN, RD, PosA, PayA, TaxA`

## LSports Interview Prep
- יצרנו diagram מלא של ה-pipeline ב-HTML: `/data/.openclaw/workspace/lsports-flow.html`
- Flow: Scout/Bookmaker/Statscore → Adapters → Kafka raw-events (24p) → Normalization → Kafka normalized-odds (50p) → Distribution/ClickHouse/Anomaly → RabbitMQ → Clients
- E2E latency: ~82ms

## WhatsApp — ליגל
- עופר ביקש להפסיק לענות להודעות מליגל (+972507470827) — NO_REPLY תמיד
- הוסר ה-job והוואטסאפ הורד מצידה

## הנחיית תקשורת
- עופר אמר "לא ביקשתי למה אתה כותב לי" — לא לשלוח דברים יזומים שלא התבקשו
